__pycache__/
*.pyc
*.pyo
*.bak
*.tmp
.DS_Store
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
# pytest-cov 并行数据碎片：.coverage.<host>.pid<N>.<rand>。
# 只写 .coverage 认不出它；防误提交覆盖率碎片。
.coverage.*
# 不带点前缀的覆盖率产物：coverage json/xml 写到仓库根，上面两条认不出。
coverage.*
.scratch/
dist/
data/
# 本地虚拟环境（uv 默认名）：仓库根的 venv 不属于源码；此前漏列正是
# scripts/gates.py 需要维护 40 行 tracked-files 文件列表喂 ruff 的根因。
.venv/
node_modules/
output/playwright/
playwright-report/
test-results/
uv.lock
