# 主动回复

白名单会话内的 AstrBot 主动接话插件。

当前版本：`0.7.17`

插件会在白名单会话收到普通聊天消息后，等待一段静默时间，再通过“判断模型”决定是否适合主动接一句。真正生成回复时不走裸模型调用，而是复用 AstrBot 主 Agent / tool loop 管线；主动运行会单独收紧工具边界，默认不继承第三方插件工具、跨会话消息、文件、Shell/Python、浏览器、定时任务、技能管理和记忆写入能力。

直接 `@Bot`、唤醒词和指令仍交给 AstrBot 主回复链处理，避免主动回复和正常回复同时触发。配置和状态文件跟随 AstrBot 当前数据根目录；跨平台群聊状态按完整 UMO 保存，旧的裸群号白名单仍作为兼容写法接受。

## 工作方式

1. **触发收集**
   - 插件监听白名单会话中的普通聊天消息。
   - 明确要求 Bot 接话、发表情包、发图、找图、来个动图等内容会优先放行。
   - 普通聊天会进入判断模型。

2. **判断模型只负责是否接话**
   - 判断模型使用可配置提示词输出严格 JSON：
     ```json
     {"should_reply": true, "reason": "一句简短理由"}
     ```
   - 它不负责生成正文，也不负责调用工具。

3. **回复生成走 AstrBot 主 Agent**
   - 主动回复正文通过 AstrBot 主 Agent / tool loop 生成。
   - 主动请求从空工具集开始，并临时限制宿主的插件工具范围；宿主在 build 阶段注入的工具（如 Web 搜索、跨会话发送、MCP）会在 reset 前和运行前被再次强制清空。默认允许列表为空，无法枚举或无法清空工具集时，该次主动 Agent 直接拒绝运行（fail closed）并记录原因。
   - 主动回复不会默认调用 Stealer、LivingMemory 或其他第三方插件工具，也不会向其他会话发消息。


## 工具边界

主动回复和用户明确 `@Bot` 的正常回复是两条不同的运行路径。主动回复默认关闭第三方插件工具、Shell/Python、文件读写、浏览器、cron、技能管理、记忆写入和跨会话发送；最终发送也受本次运行的直发预算与 generation 校验约束。这样可以避免群聊中的普通消息借助主动回复间接获得宿主机或其他会话能力。

插件使用 AstrBot 主 Agent 私有入口，加载时检查 `ToolSet`、Agent 构造函数和运行函数签名；工具策略在 build 与 hook 之后各强制执行一次，策略无法验证的版本会在加载期报错或在运行期终止本次主动 Agent。当前已验证 AstrBot `4.26.8`，`metadata.yaml` 声明 `>=4.26.8,<5`；更早版本的兼容性只在完整版本矩阵验证通过后才会放开。

## 图片识别（可选）

默认关闭。开启后，插件会在判断是否接话以及生成回复时，带上最近若干张图片的文字描述：

- 两个识图开关独立控制：**判断模型识图**（判断是否接话时看图）和**主模型识图**（生成回复正文时看图）。判断阶段触发频率高，只开主模型识图更省 token；两者均可独立开关，均需支持 Vision 的模型。
- 可开启**跳过表情包识图**：按消息段元数据识别表情包，QQ OneBot 的 `subType=1` 会被直接排除，不调用 Vision，也不会把它放进图片缓存；普通图片和普通 GIF 不受影响。
- 判断阶段可单独指定识图模型 Provider：判断阶段触发频率远高于主阶段，可以配置一个更便宜的 Vision 模型降低成本。留空时与主识图模型保持一致，两者指向同一 Provider 时图片只描述一次。
- 图片识别直接调用所选 Provider，不经过其他 LLM 钩子，避免与记忆/识图类插件互相递归。
- 收到图片事件时，先把 AstrBot 已归一化的宿主临时图片快照到插件自动创建的 `image_cache` 目录；远程 URL 再由后台任务下载冻结，主动回复的延迟窗口只读取缓存副本，不再依赖可能过期的 QQ CDN URL。
- 优先使用 message_recorder 的本地图片；网络图片仅允许公网地址、跟随必要的 CDN 重定向并在最终响应上再次校验，限制大小，降低 SSRF 风险。
- 无论本地文件还是网络图片，都按魔数字节校验真实图片格式（JPEG/PNG/GIF/WebP/BMP）。文件名、扩展名和 `content-type` 只作参考，因此适配器给出的任意本地路径不会被当成图片读取并外传。
- 图片描述只作为「不可信聊天上下文」参与判断与生成，不会写入会话历史或状态文件，其中的文字不能改变任务边界或触发工具。
- 相同图片会按实际 Vision Provider 命中当前运行期的内存描述缓存，避免不同 Provider 之间串用描述；图片源按内容寻址写入 `image_cache`，保证延迟期间可复用。独立清理任务会按图片有效时间回收未被有效事件引用的源文件，缓存同时受总容量上限约束，临时事件索引会在插件重载时清空。
- 插件加载/重载时会立即扫描一次自己的 `image_cache`；运行期间继续按周期自动清理。设置页面“图片识别”区域提供“立即清理过期图片”按钮，会按有效期和总容量上限清理未被当前任务保护的插件缓存，不会清理 AstrBot 全局临时目录，也不会删除仍在有效窗口内的受保护图片。

可调字段：启用开关、识图 Provider（主阶段与判断阶段可分别指定）、跳过表情包识图、单次最多识别图片数、图片有效时间、识图超时。

## 设置页面

插件详情页中的设置页面用于管理主动回复自身的触发和判断逻辑，主要包含：

- 插件运行状态和白名单数量
- 主动回复运行开关
- 最少上下文消息数、消息后延迟秒数、最小静默秒数、冷却秒数
- 判断模型 Provider 选择/手动输入、温度、超时秒数
- 判断提示词、实时预览与恢复默认提示词按钮
- 可展开的提示词变量帮助
- 高级白名单 UMO 批量编辑
- 图片缓存过期/超额清理按钮及执行结果提示

### 判断模型调参

页面中的“判断模型调参”区域是最常用的调试入口。

可调字段：

- **启用提示词判断模型**：关闭后普通消息不会由判断模型主动接话；明确接话/表情包请求仍可按规则放行。
- **判断模型 Provider**：默认使用当前会话模型；也可以从已有 LLM Provider 下拉选择，或切换到手动输入填写特殊 Provider ID。
- **判断温度**：建议 `0.0 - 0.3`，越低越稳定。
- **判断超时秒数**：判断模型最大等待时间，默认建议 `20` 秒。
- **判断提示词**：控制“什么时候该主动接话”。右侧预览会用示例变量实时渲染，方便检查最终提示词形态。

提示词可用变量：

```text
{session}                 # 当前会话 UMO，用于区分群聊/私聊/平台
{trigger}                 # 触发类型，例如 message、patrol、manual
{bot_aliases}             # Bot 名称/别名列表，便于判断用户是否在叫 Bot
{latest_message}          # 最近一条用户消息文本
{recent_messages}         # 最近上下文消息，通常是判断是否接话的主要依据
{last_message_age_sec}    # 距离最近一条消息过去的秒数
{last_reply_age_sec}      # 距离本插件上次主动回复过去的秒数
```

如果提示词留空保存，会恢复默认提示词。

预览窗口只使用页面内置的示例变量，不读取真实群聊消息；实际运行时，插件会根据当前会话、触发方式和最近消息替换这些变量。

修改配置后点击页面顶部或底部的“保存”。保存成功后页面会回读服务端配置；如果重载插件或重启 AstrBot 后仍要查看最新页面资源，建议在浏览器中硬刷新。

## 指令

为避免日常中文聊天误触发管理指令，插件只注册英文命令入口。

| 指令 | 说明 |
| --- | --- |
| `/selfreply` | 查看帮助 |
| `/selfreply status` | 查看运行状态、判断模型和白名单信息 |
| `/selfreply add` | 将当前会话加入主动回复白名单 |
| `/selfreply remove` | 将当前会话移出主动回复白名单 |
| `/selfreply list` | 查看白名单 |
| `/selfreply check [content]` | 手动测试一次主动回复，可附带测试内容 |
| `/selfreply on` | 临时启用主动回复 |
| `/selfreply off` | 临时暂停主动回复 |
| `/selfreply debug` | 查看当前会话、发送者和触发识别信息 |

也支持 `@Bot selfreply add` 这类管理写法；普通 `@Bot` 聊天不会触发主动回复插件，而是交给 AstrBot 正常回复链。

`/selfreply on/off` 是临时运行态：在 AstrBot 官方 Dashboard 保存本插件任意配置会触发插件热重载，临时状态会被重置为持久配置值；插件自带设置页保存配置不会触发重载。

## 关于白名单

主动回复白名单和 AstrBot 全局会话白名单是两层不同限制：

- AstrBot 全局白名单会先于插件生效。
- 如果某个群没有通过 AstrBot 全局白名单，消息会在到达插件前被核心拦截。
- 本插件白名单只决定“是否允许主动回复”。

在群聊中使用 `/selfreply add` 会把整个群会话加入白名单，而不是只加入当前发言者。

页面里仍保留“高级：白名单会话 ID”区域，适合批量编辑。群聊可以填写完整 UMO，也可以直接填写 QQ 群号；日常更建议使用 `/selfreply add` 和 `/selfreply remove`。

## 建议配置

- **判断温度**：建议 `0.0 - 0.3`。
- **判断超时秒数**：建议 `10 - 30` 秒。
- **最少上下文消息数**：建议 `3 - 8`，默认可保持 `5`。
- **消息后延迟秒数**：建议 `60 - 180` 秒，测试时可临时调低。
- **最小静默秒数**：建议 `120 - 300` 秒；群里持续有人说话时不会主动回应。
- **冷却秒数**：建议 `300 - 900` 秒，避免刷屏。
- **每日每会话最大回复次数**：建议 `5 - 12` 次，`0` 表示不限。
- **主动回复最长字符数**：建议 `100 - 300` 字符，`0` 表示不限制。
- **图片识别**：默认关闭；开启后主动回复会带上最近图片的文字描述，需要使用支持 Vision 的模型。若群里表情包较多，可同时开启“跳过表情包识图”。
- **忽略发送者 ID**：被列入的用户不会触发主动回复，包括管理员；仅用于明确屏蔽特定发送者。指令路径（如 `/selfreply status`）不受忽略列表影响。

## 排查建议

如果没有主动回复：

1. 确认当前会话已加入主动回复白名单。
2. 确认 AstrBot 全局白名单没有拦截该会话。
3. 查看 `/selfreply status`。
4. 查看日志中的 `should_reply` 和 `reason`。
5. 如果判断模型经常不放行，优先调整页面里的“判断提示词”和“判断温度”。

修改插件代码、页面或 metadata 后，建议重启 AstrBot 或重载插件让改动生效。
