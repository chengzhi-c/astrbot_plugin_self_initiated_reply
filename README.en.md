<p align="center">
  <picture>
    <source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/chengzhi-c/astrbot_plugin_self_initiated_reply/main/assets/banner-dark.jpg">
    <source media="(prefers-color-scheme: light)" srcset="https://raw.githubusercontent.com/chengzhi-c/astrbot_plugin_self_initiated_reply/main/assets/banner-light.jpg">
    <img alt="Yejing · Proactive Reply" src="https://raw.githubusercontent.com/chengzhi-c/astrbot_plugin_self_initiated_reply/main/assets/banner-light.jpg" width="100%">
  </picture>
</p>

<h1 align="center">Yejing · Proactive Reply</h1>

<p align="center">
  <em>Yejing (业镜, lit. "Mirror of Karma")</em> — a plugin that lets AstrBot naturally chime into conversations in whitelisted sessions.<br>
  <img alt="Version" src="https://img.shields.io/badge/version-0.9.1-4a5580">
  <img alt="AstrBot plugin" src="https://img.shields.io/badge/AstrBot-plugin-7b86ab">
</p>

<p align="center">
  <strong><a href="#table-of-contents">Table of Contents</a></strong> · <a href="README.md">中文版</a>
</p>

---

<a name="table-of-contents"></a>
<details markdown="block">
<summary>📑 Table of Contents (click to expand)</summary>

- [Overview](#overview)
- [How It Works](#how-it-works)
- [Tool Boundaries](#tool-boundaries)
- [Image Recognition (Optional)](#image-recognition-optional)
- [Settings Page](#settings-page)
- [About the Whitelist](#about-the-whitelist)
- [Commands](#commands)
- [Recommended Configuration](#recommended-configuration)
- [Troubleshooting](#troubleshooting)
- [Security Notes](#security-notes)
- [Summary](#summary)

</details>

---

<a name="overview"></a>

## 📖 Overview

> **Yejing (业镜, "Karma Mirror")** — named after Enma's "Karma Mirror" card in *Onmyoji: Shadows of the Yin (阴阳师：百闻牌)* — lets AstrBot take part in conversations on its own, proactively replying in whitelisted sessions instead of only responding when called by name.

The core idea in four points:

- After a message arrives, the plugin waits for the conversation to go quiet, then asks a **judge model** whether it should chime in.
- Proactive replies are produced by AstrBot's main Agent pipeline, but with **tool boundaries tightened by default** (they can be relaxed).
- Direct `@Bot` mentions, wake words, and commands keep using the normal reply chain — the two paths never interfere with each other.
- All configuration and runtime state live under AstrBot's data-root directory. Cross-platform group chats are stored as full **UMO** (Universal Message Object) entries, while bare group IDs remain supported for backwards compatibility.

> 💡 **Where to start:** open the [Settings Page](#settings-page), then apply the [Recommended Configuration](#recommended-configuration) to get your first proactive reply running.

---

<a name="how-it-works"></a>

## 🚀 How It Works

The plugin runs as a three-stage pipeline:

1. **Collect** — Listens to ordinary messages in whitelisted sessions. Content that explicitly asks to reply, send an image, or find an image is passed through immediately; everything else is handed to the judge model.
2. **Judge** — The judge model answers only one question — *reply or not* — and returns strict JSON:
   ```json
   {"should_reply": true, "reason": "a brief reason"}
   ```
3. **Reply** — The reply body is generated by AstrBot's main Agent / tool loop, using the full toolchain but with restricted boundaries.

---

<a name="tool-boundaries"></a>

## 🔒 Tool Boundaries

Proactive replies and `@Bot` normal replies are two **independent paths**.

By default, the proactive path disables:

- third-party plugin tools,
- Shell / Python execution,
- file read & write,
- the browser,
- cron,
- skill management,
- memory writes,
- and cross-session sending.

When the available tool set cannot be verified, the plugin **refuses to run outright (fail closed)**. The settings page offers an **"Allow inheriting plugin tools"** option to relax this boundary — but host-level dangerous capabilities (cron, browser / computer use, and file extraction) remain disabled under all circumstances.

---

<a name="image-recognition-optional"></a>

## 🖼️ Image Recognition (Optional)

Off by default. When enabled, text descriptions of the most recent images are attached during the judge or reply-generation phase.

- **Two independent switches.** **Judge-model vision** looks at images when deciding whether to reply; **main-model vision** looks at images when generating the reply body. The judge phase fires frequently, so enabling only main-model vision saves tokens. Each switch can use its own Provider; within the same Provider, an identical image is described only once and the result is cached in runtime memory per Provider (~50 entries, re-described after a plugin reload/restart or cache eviction), preventing descriptions from leaking across Providers.
- **Stickers can be skipped.** QQ OneBot `subType=1` stickers are excluded outright without calling Vision.
- **Safe snapshotting.** Image snapshots go into the plugin's own `image_cache`; remote images are frozen in the background so they don't depend on expiring QQ CDN links. Real file formats are validated by magic-number checks, guarding against SSRF and forged paths.
- **Untrusted context only.** Descriptions participate in judging solely as untrusted context — they are never written to history or state and can never trigger tools. The cache auto-cleans by expiry and capacity, and the settings page provides a **"Clear now"** button.

---

<a name="settings-page"></a>

## ⚙️ Settings Page

A settings page lives inside the plugin detail page, covering:

- the run switch and whitelist overview;
- context / delay / silence / cooldown parameters;
- the judge model (Provider, temperature, timeout, and prompt editing with live preview);
- batch editing of whitelist UMO entries;
- and image-cache cleanup.

The UI ships with light and dark themes — light **"Boon of Compassion"**, dark **"Court of Judgment"** — toggled from the top-right corner and defaulting to your system preference:

<p align="center">
  <img src="https://raw.githubusercontent.com/chengzhi-c/astrbot_plugin_self_initiated_reply/main/assets/慈爱之惠.jpg" alt="Light theme · Boon of Compassion" width="49%">
  <img src="https://raw.githubusercontent.com/chengzhi-c/astrbot_plugin_self_initiated_reply/main/assets/审判之司.jpg" alt="Dark theme · Court of Judgment" width="49%">
</p>

**Judge model tuning** is the most frequently used entry point:

| Field | Description |
| --- | --- |
| Enable judge model | When off, ordinary messages no longer trigger proactive replies; explicit requests can still pass through |
| Judge model Provider | Defaults to the current session model; pick from the dropdown or enter manually |
| Judge temperature | Recommended `0.0 – 0.3`; lower is steadier |
| Judge timeout (seconds) | Recommended `20` seconds |
| Judge prompt | Supports `{session}` `{trigger}` `{bot_aliases}` `{latest_message}` `{recent_messages}` `{last_message_age_sec}` `{last_reply_age_sec}` variables, with live preview on the right; leave empty and save to restore the default |

After changing settings, click **Save** and the page re-reads the server-side config. To see the latest page assets after reloading the plugin or restarting AstrBot, hard-refresh the browser.

---

<a name="about-the-whitelist"></a>

## 🎯 About the Whitelist

The whitelist has **two layers**:

1. AstrBot's **global whitelist** intercepts first.
2. Only after passing it does the **plugin whitelist** decide whether to reply proactively.

`/selfreply add` adds the *entire group session*. The **"Advanced: Whitelist Session IDs"** section supports batch editing — you may enter either a full UMO or a bare group ID.

---

<a name="commands"></a>

<details markdown="block">
<summary>📋 Commands (7 total, click to expand)</summary>

Only English command entries are registered, to avoid accidental triggers in daily chat:

| Command | Description |
| --- | --- |
| `/selfreply` | Show help |
| `/selfreply status` | Runtime status, judge model, whitelist |
| `/selfreply add` / `remove` | Add / remove the current session from the whitelist |
| `/selfreply list` | View the whitelist |
| `/selfreply check [content]` | Manually test one proactive reply |
| `/selfreply on` / `off` | Temporarily enable / pause |
| `/selfreply debug` | Current session, sender, and trigger-recognition info |

Forms like `@Bot selfreply add` are also supported.

> ⚠️ Note that `/selfreply on` / `off` is a **temporary runtime state**: saving plugin config in the official Dashboard triggers a hot reload that resets the temporary state, whereas saving from the plugin's own settings page does not.

</details>

<a name="recommended-configuration"></a>

<details markdown="block">
<summary>✅ Recommended Configuration (click to expand)</summary>

| Parameter | Recommendation |
| --- | --- |
| Judge temperature | `0.0 – 0.3` |
| Judge timeout | `10 – 30` seconds |
| Minimum context messages | `3 – 8` (default `5`) |
| Delay after message | `60 – 180` seconds (lower for testing) |
| Minimum silence seconds | `120 – 300` seconds |
| Cooldown seconds | `300 – 900` seconds |
| Max replies per session per day | `5 – 12` (`0` = unlimited) |
| Max proactive reply length | `100 – 300` chars (`0` = unlimited) |
| Image recognition | Off by default; groups heavy on stickers can enable "skip sticker images" |

- **"Ignore sender ID"** can block specific users (including admins); the command path is unaffected.
- **"Log proactive reply content"** is off by default to protect privacy; enable it in settings when troubleshooting.

</details>

<a name="troubleshooting"></a>

<details markdown="block">
<summary>🛠️ Troubleshooting (no proactive replies? click to expand)</summary>

Check in order:

1. Is the session in the proactive-reply whitelist?
2. Is AstrBot's global whitelist not intercepting it?
3. Look at `/selfreply status`.
4. Check `should_reply` and `reason` in the logs.
5. If the judge model isn't letting it through, tune **"judge prompt"** and **"judge temperature"**.

After changing plugin code, pages, or metadata, restart AstrBot or reload the plugin to apply the changes.

</details>

---

<a name="security-notes"></a>

## 🔐 Security Notes

> ⚠️ **Security warning:** the plugin's settings page and Web API (e.g. `/api/plugin/selfreply/config`) do **not** provide independent authentication. Access control relies entirely on the host AstrBot Dashboard's login and permission system.

- Do **not** expose AstrBot's web port to untrusted networks.
- Changes to security-sensitive config (e.g. `proactive_inherit_tools`, whitelist) are recorded in the plugin **INFO** logs for later audit.

---

<a name="summary"></a>

## 📌 Summary

Yejing keeps AstrBot helpful without being intrusive: a lightweight judge model decides *when* to speak, the main Agent decides *what* to say, and strict tool boundaries keep every proactive action safe by default. Start from the [Settings Page](#settings-page) and the [Recommended Configuration](#recommended-configuration), then use [Commands](#commands) and [Troubleshooting](#troubleshooting) to fine-tune and diagnose. Remember to keep the Dashboard behind trusted access — see [Security Notes](#security-notes).
