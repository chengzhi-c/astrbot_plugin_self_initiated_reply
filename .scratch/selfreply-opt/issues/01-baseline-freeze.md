# 01 — 基线冻结与行为契约

**What to build:** 把当前行为契约钉死，使后续拆分阶段可安全进行而不产生行为漂移。两个交付物：模块级覆盖率门槛（主插件与图片解析链路单独按模块 fail，防止整体门槛掩盖局部盲区）、隐式不变量文档（作为后续重构的判定依据）。

**Blocked by:** None — can start immediately

**Status:** resolved

## Resolution (2026-08-06)

- 模块级门槛：新增 `scripts/coverage_gates.py`（按文件粒度，生产口径，阈值=实测−5%），
  CI test 作业在 pytest 后强制；红/绿两条路径均已实测验证。
- 行为契约：`docs/BEHAVIOR_CONTRACT.md`，10 类不变量 + 冻结基线
  （0.8.6，287 tests，75.79% 整体，main.py 60%、parser.py 63%）。
- 验证：pytest 287 全绿；门槛脚本 PASS/FAIL 双路径自检通过；ruff 全清。

- [ ] 覆盖率门槛改为模块级：主插件、图片解析链路分别设置下限，低于即 CI 红灯（基线实测：主插件 60%、解析链路 63%，整体 75.79%）
- [ ] 行为契约文档入库，至少覆盖：代次单调性与观察窗口推进、UNKNOWN 投递不重试但消耗冷却与日配额、工具策略 fail-closed、静默/冷却/免打扰/日配额四层局部闸门的判定顺序、终止时的落盘保证
- [ ] 全部 287 测试绿、整体覆盖率 ≥70% 保持，基线数字（时间、版本、覆盖、测试数）记录进文档
