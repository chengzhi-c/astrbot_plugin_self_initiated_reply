# 03 — 拆分判断链路（decision）

**What to build:** 把"是否接话"的裁决逻辑迁出为独立模块：判断模型调用（含超时/失败分类）、判断提示词构建与注入清理、明确请求窗口检测、局部闸门判定（免打扰/日配额/静默/冷却/观察窗口）。对外只暴露一个裁决入口，入参为会话状态与触发类型，出参为"回复/跳过+原因"。

**Blocked by:** 01 — 基线冻结与行为契约

**Status:** resolved

## Resolution (2026-08-06)

- 新增 `decision.py`（DecisionMaker，346 行）：裁决入口 `decide`（入参会话状态
  + 触发类型，出参"回复/跳过+原因"）、判断模型调用（超时/失败分类）、提示词
  构建与注入清理、明确请求窗口检测、局部闸门（免打扰/日配额/静默/冷却/观察窗口）。
  桥接（provider 解析/模型生成/历史读取/Vision 描述）经注入回调执行。
- main.py 2017 → 1840 行（-8.8%，含委托壳与决策日志；体量收益主要依赖
  04-06 继续摊薄 + 07 收尾移除）。
- 新增 `tests/test_decision_maker.py`（28 用例）：注入假判断模型 + 假时钟
  （clock/minutes_now），四类降级路径文案逐字断言（验收项 3 达成）；
  提示词注入清理（用户片段引号改写、JSON 契约追加、多行保留）实测（验收项 2）。
- 日志锚定保持：decision 结果日志留在 main.py 委托壳，round6 源码护栏不动。
- 实测：391 tests 全绿（+28），整体 82.72%，decision.py 覆盖 97%，
  模块门槛 92 已加入 `scripts/coverage_gates.py`，ruff/mypy 全清。
