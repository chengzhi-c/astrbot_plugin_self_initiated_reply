repos:
  # lint/type 检查不在 pre-commit 里重复：本地走 `uvx ruff` / `python scripts/gates.py`，
  # CI（唯一权威）钉版见 ci.yml lint 作业与 pyproject [dev]。
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: end-of-file-fixer
      - id: trailing-whitespace
