# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的格式。

## [Unreleased]

- 新增主动回复「引用消息」开关：`quote_mode`（不引用 / 由判断模型决定 / 按概率引用）
  与 `quote_probability`，引用的是本次回复依据的最后一条消息；取不到消息 ID 或平台
  不支持时自动降级为普通发送。判断模型的裁决 JSON 新增可选 `quote` 字段（不影响
  旧提示词的解析）。
- 新增「明确接话请求也交给判断模型」开关（`reply_request_requires_model`，默认关）：
  此前「在吗 / 有人吗 / 说句话 / 发表情包」这类请求直接接话、完全不经过判断模型，
  判断提示词对它们从不生效——这是"提示词像是没被看"的主因。
- 新增「被点名后本批消息不再主动接话」（`skip_after_direct_call`，默认开）：@Bot 由
  宿主管线回复后，同一批消息不再追加主动回复，下一条新消息到达即恢复。
- `/selfreply status` 新增「最近裁决」一行（时间、接话/不接话、原因，原因折单行
  并截断 60 字）：与运维端点 `GET /status` 的 `last_decisions` 同一份数据，
  排障时不必再去翻日志。
- 修复判断提示词上下文超预算时截头而非截尾的问题：此前会先丢掉最新几条，与默认
  模板「优先参考最近至少 8 条」相反，模型看到的是最不相关的旧消息。
- 修复设置保存后白名单计数显示提交前数量的问题，现以服务端归一化结果为准。
- 修复保存校验时错误焦点被二次抢夺导致的提示跳变，白名单问题优先短路返回。
- 会话无任何活动记录时，闸门提示改为"会话暂无活动记录，无法判断静默"，
  不再误报"静默时间不足"。
- 修复直接点名识别的数字边界缺陷：他人 QQ 号以本机账号结尾（如
  `[CQ:at,qq=456123]` 命中 self_id=123）不再被误判为点名而导致消息静默丢弃。
- 修复设置 API 提交非整数数值（如 `1.5`、`"5"`）被静默截断的问题，现返回 400。
- 修复配置应用失败回滚时会话历史的对象身份与裁剪问题：在途任务不再写孤儿
  状态，应用窗口内被裁剪的历史可完整恢复。
- 修复降级状态下 `check` 指令误报"插件未启用"的问题，现明确提示需重启插件。
- `status`/`debug` 只读指令不再为非白名单会话隐式创建内存状态条目。
- 记录落盘失败重试改为真实退避间隔，不再形同虚设。
- `ui_prefs.json` 读取改用与状态文件一致的 BOM 兼容编码，外部编辑不再丢偏好。
- 设置页压暗/粗体随主题写入 `ui_prefs.json`，重开页面仍生效。
- 图片内存预算改为热路径增量计数。
- 同一会话检查进行中再来消息时，只保留一次后续判断，避免判断模型叠跑。
- 补充前端跨源契约守卫：移动端 tab 分组与响应式断点不再可能与页面/样式表漂移。
- 补充"图片抓取失败不阻断消息调度链"的回归用例（此前只由代码结构成立）。
- 修正插件停止过程中被抑制的回复回报“插件未启用”的文案口径：现与发送阶段同一成因
  统一回报“插件正在停止，放弃回复”，不再把停机误导向改配置排查。
- 修复深色主题下 11–12px 提示文字对比度不足：`--faint` 在 `--surface-2` 底只有
  4.48:1，提亮到 4.97:1，两种主题 × 两种底色均满足 WCAG AA 4.5:1（新增实测断言）。
- 新增变异门禁（`scripts/mutation_gate.py`，接入本地 `gates.py` 与 CI 独立作业）：
  每条承重变异必须让指定测试变红，把“新测试要能捕获目标缺陷”从人工纪律变成断言。
- 补齐三处契约缺口的回归用例：局部闸门的归因顺序（§4）、UNKNOWN 的代次门（§2）、
  图片重定向上限（§8）、面板 `config_revision` 的格式校验；四者均已在变异门禁登记。
- 新增「文档指令清单 ↔ 解析器」同源守卫：`README.md` / `metadata.yaml` help /
  `help_text()` 里出现的 `/selfreply` 指令必须真能解析，且不漏任何动作。
- 删除纯中转的会话字段 `last_active_sender_id`（写 3 处、读 0 处），旧 `state.json`
  里多出的键仍可正常加载，不需要状态版本迁移。
- 修复源码分发包（sdist）检查自身误报导致的发布门禁恒红：`scripts/check_sdist.py`
  的注释里带盘符字面量做例子，而该文件本身在 sdist 内、会被自己的机器路径扫描命中
  （干净 HEAD 上重建 sdist 即可复现）。现在注释不写盘符字面量，扫描逻辑与判据不变。

## [1.3.3] - 2026-09-03

- 优化图片缓存逐出逻辑，消除大批量消息时的重复扫描与解码开销。
- 修复取消延迟任务时的静默事件残留，以及本地图片快照异常可能阻断后续调度的边界问题。
- 前端设置页白名单校验支持精准定位错误行号，并改进 Provider 模式切换与主题同步。

## [1.3.2] - 2026-08-25

- 新增「新消息到达放弃旧回复」开关（`abandon_stale_on_new_message`，默认关）。
- 修复回复生成途中收到新消息导致已通过回复被静默拦截的问题。

## [1.3.1] - 2026-08-25

- 设置页「去重并整理」支持自动合并裸群号对应的群 UMO。
- 设置页空数值项自动回填上次已保存的值。
- 修复投递记账状态更新时序。

## [1.3.0] - 2026-08-24

- 新增「私聊主动回复」独立开关（`enabled_private_sessions`，默认开）。
- 优化设置页控件布局与空白状态提示。

## [1.2.0] - 2026-08-15

- 重构图片识图运行时为独立服务，优化图文上下文生成时序。
- 设置页从元数据派生可写配置项，降低前后端配置不一致风险。
- 完善工程质量门禁与跨平台回归测试。

## [1.1.1] - 2026-08-13

- 含图消息接收时异步快照到本地，避免阻塞事件循环。
- 手动检查指令记录发送者，白名单校验规则与后台对齐。

## [1.1.0] - 2026-08-12

- 设置页补充多端真实浏览器回归测试，覆盖异常与回退场景。
- 模块结构解耦，拆分会话、调度与图片处理路径。

## [0.9.7] - 2026-08-12

- 设置页加载失败时禁止写回默认值，避免覆盖服务端配置。
- 前端页面重构为原生 ESM 模块，优化加载性能与窄屏交互。

## [0.9.6] - 2026-08-11

- 改进消息发送取消时的记账逻辑，避免可能已送达的消息被重复发送。
- 完善 Provider 列表不可用时的降级回退机制。

## [0.9.5] - 2026-08-10

- 修复 AstrBot 新版宿主加载期类型注解解析兼容性问题。
- 保护消息发送管道，避免覆盖第三方插件的发送包装。

## [0.9.4] - 2026-08-10

- `/selfreply on` / `off` 开关状态直接持久化，重启后依然生效。
- 增强提示词拼接与时间戳解析的边界防御。
- 规范版本号事实源，精简分发包体积。

## [0.9.3] - 2026-08-08

- 收紧本地图片来源校验，仅放行合法数据目录下的文件。
- 精简分发包资源，大幅降低打包体积。
- 强化配置变更审计日志。

## [0.9.2] - 2026-08-08

- 统一配置接口正式字段，向下兼容过渡旧字段。
- 随包前端页面全面切换正式配置项。

## [0.9.1] - 2026-08-07

- 设置页新增「压暗亮度」与「粗体字」无障碍显示偏好。
- 优化明暗主题细节、触控区域高度与中西文混排样式。

## [0.9.0] - 2026-08-07

- 修复配置热更新时多组件实例数据不同步的问题。
- 简化内部模块划分，移除无用死代码。

## [0.8.0] - 2026-08-04

- 默认关闭回复内容日志记录以保护隐私。
- 优化只读指令处理性能，规范工程检查流水线。
