# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的格式。
小版本（0.8.1~0.8.8）迭代细节省略，详见 git 历史。

## [0.9.5] - 2026-08-10

### 修复

- **在 AstrBot 4.27.2 上无法安装。** 指令处理器的返回类型注解用了只在类型检查期存在的
  名字，新版宿主加载期会真去解析这些注解。改为运行时可解析，4.23.3 与 4.27.2 均可安装。
- **不再破坏同一条消息上其他插件的发送钩子。** 主动回复收尾时只还原确认是自己装上的
  发送包装，避免误删第三方插件的包装导致其静默失效。

## [0.9.4] - 2026-08-10

以「外部输入不可信」为主线的一批修复。无配置迁移。

### 行为变更

- **`/selfreply off` 与 `/on` 现在跨重启保持。** 此前只改内存运行态，宿主一重启就回落；
  现在开关直接落盘，回显也写明「重启后保持」。

### 安全

- **群聊内容不能再撑开提示词信封。** 发一条含 `</recent_chat>` 的消息即可提前闭合信封、
  让后续文字与插件自身指令同级，现改为在拼装处中和信封标签；聊天记录里的代码片段不受影响。
- **状态文件时间戳两侧钳位。** 负值与远未来值不再穿透，避免会话被永久锁死或配合其他
  字段绕过「这条消息之后已回复过」的判据。

### 修复

- 配置回滚导致检查闸门失同步时不再永久挂起，降级为一次延迟或一次丢弃。
- 钩子抛异常时不再遗留未回收的 reset 协程。
- 读不到宿主工具集时中止主动运行（fail closed），不再当作「没有工具」放行。
- 调试面板的状态接口补上异常兜底。
- 静默不足文案不再出现负数。

### 分发

- 版本号收敛为单一事实源（`models.PLUGIN_VERSION`），wheel 文件名与 dist-info 由它派生。
- 覆盖率产物、`*.egg-info/`、嵌套 `__pycache__/` 不再进 wheel。
- 开发依赖补齐版本上界。

## [0.9.3] - 2026-08-08

分发瘦身与结构收敛。无配置迁移、无命令契约改动，**但有三处真实行为变更**。

### 行为变更

- **本地图片放行判据收紧（安全修复）。** 原先采信提取层的可信推断，图片 `file` 字段可被
  对端伪造成任意绝对路径，构成任意文件读取。改为只认路径落在 `<data>` 与插件缓存目录内。
  **已知影响**：Telegram 配自建 Bot API 服务器时图片被拒 → 降级为纯文本主动回复
  （默认 HTTPS 配置不受影响）。
- **配置回滚不再让协作对象写孤儿容器。** 修复后该会话主动回复不再静默停止直到重启。
- **白名单双写回滚还原被摘下的会话状态。** 持久化失败时不再丢失配额、冷却与观察窗口。

### 分发

- `assets/` 4 张 banner 不再进 wheel（**687,531 → 223,340 B，-67.5%**），资产仍留在 git 仓库。

### 安全与可观测

- WebAPI 配置审计键 3 → 9：Provider、Vision 开关与忽略名单的变更均记 INFO 审计日志。
- 图片下载失败日志不再记录 URL 中的签名凭证。

## [0.9.2] - 2026-08-08

### 行为变更

- WebAPI 配置接口只接受正式键：6 个兼容别名（`cooldown_seconds` /
  `idle_trigger_seconds` / `min_context_messages` / `proactive_threshold` /
  `vision_enabled` / `whitelist`）移除，旧前端发别名会得到显式「未知配置键」错误。
- GET config / unified/overview 响应只返回正式键。
- check result 判断日志升为 INFO。

### 兼容迁移

- 存量配置文件含别名键时不丢值：读侧回退到正式键，一次 load+save 后别名自然消失。
- 随包前端页面全面切换正式键。

## [0.9.1] - 2026-08-07

仅表现层改动，后端逻辑、配置 schema 与命令契约零变更。

### 新增

- 顶栏「压暗亮度」「粗体字」两个显示偏好开关，状态持久化并暴露 `aria-pressed`。

### 缺陷修复

- 压暗只覆盖半个页面：固定定位的保存条、Tab 栏、Toast 未纳入滤镜。
- 亮色主题下按钮出现贴顶白线；显示偏好按钮的开关态与悬停态不可辨。
- 提示词编辑区中西文混排抖动；折叠标题图标与文字零间距。

### 界面调整

- 字号阶梯按职能重排、控件高度提至 44px 触控档位、保存按钮补图标。

## [0.9.0] - 2026-08-07

### 缺陷修复

- 配置热更新不再分裂：五个组件统一经同一引用读到最新配置。
- delayed_check 早退路径不再 UnboundLocalError。

### 内部结构

- 模块合并：unified_manager → webapi、events → utils；删除死代码。

## [0.8.0] - 2026-08-04

### 行为变更

- 隐私默认收紧：`log_reply_content` 默认改为 false。
- 只读指令（status/list/help/debug/check）不再使缓存失效；高频日志降 DEBUG。

### 修复与工程化

- 运行中检查 force cancel 时不再留下孤儿 run_agent；context 兜底发送不再误记 UNKNOWN。
- 拆出 webapi.py；引入 ruff/mypy/pre-commit/CI。
